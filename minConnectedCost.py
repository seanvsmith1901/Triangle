class Solution:
    def minCostConnectPoints(self, points) -> int:
        pass
        tree = [] # holds all of the points that we have so far
        # points are passed in as a set of tuples with distances between them
        # I think the easiest way to do this would be to use prims
        # whee
